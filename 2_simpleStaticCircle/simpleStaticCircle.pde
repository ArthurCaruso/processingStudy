
// este código cria um círculo que será renderizado 
// apenas uma vez estaticamente (ou seja, o programa não estará processando e 
// atualizando os frames

  void setup(){ //seta o obejeto que armazena os 
  //valores referentes a renderização da screen
    size(480, 480);
    background(#ffffff);
    fill(255, 44, 55);
    circle(240, 240, 30);
  }
  
 
  
  void render(){};
